<resources>

    <!-- Base application theme. -->
    <style name="login" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->

        <item name="colorControlHighlight">@color/button</item>
    </style>

    <style name="login.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="login.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="login.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
